import { GrandPrix } from "@/types";
import NavDropdownLayout from "@/components/layouts/nav-dropdown-layout";

export default function RaceEvents({ races }: { races: GrandPrix[] }) {
    return (
        <NavDropdownLayout>
           
        </NavDropdownLayout>
    );
}
